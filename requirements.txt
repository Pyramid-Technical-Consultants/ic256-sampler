# Core dependencies
portalocker>=2.0.0
requests>=2.31.0
websocket-client>=1.6.0
Pillow>=10.0.0

# Development dependencies (optional)
# Install with: pip install -r requirements.txt
# For dev dependencies: pip install -e ".[dev]"
