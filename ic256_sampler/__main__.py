"""Package entry point for running IC256 Sampler as a module.

This allows running the application with:
    python -m ic256_sampler

This is the standard Python way to run a package.
"""

from ic256_sampler.main import main

if __name__ == "__main__":
    main()
